import React from 'react';
import ResizableElement from './ResizableElement';

const App: React.FC = () => {
  return <ResizableElement initialWidth={200} initialHeight={150} />;
};

export default App;
